
@{
    ViewData["Title"] = "Lobby";
}

<h1>Lobby</h1>
<div class="DivStartButtons" , style="margin-top: 265px">
    <div style="margin-top: 20px">
        @Html.ActionLink("Start game", "Lobby", "Lobby", null, new { @class = "btn btn-startbuttons" })
    </div>
    <div style="margin-top: 20px">
        @Html.ActionLink("Leave lobby", "Lobby", "Lobby", null, new { @class = "btn btn-startbuttons" })
    </div>
</div>

